#
# ~/.bash_profile
#

# Source bashrc
[ -f ~/.bashrc ] && . ~/.bashrc

# Source env
[ -f ~/.config/shell/env ] && . ~/.config/shell/env

# AutoStart GUI
[ -f ~/.config/shell/GUI ] && . ~/.config/shell/GUI
